"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const log = require("loglevel");
function prepareLine(prefix, args) {
    return [prefix].concat(Array.from(args));
}
class Logger {
    constructor() {
        this.prefix = '';
    }
    static scope(prefix) {
        // TBD this.prefix += ' ' + prefix;
        return new Logger();
    }
    setLevel(level) {
        log.setLevel(level);
    }
    static setLevel(level) {
        log.setLevel(level);
    }
    trace(...args) { log.trace.apply(null, prepareLine('MCS T:' + this.prefix, args)); }
    debug(...args) { log.debug.apply(null, prepareLine('MCS D:' + this.prefix, args)); }
    info(...args) { log.info.apply(null, prepareLine('MCS I:' + this.prefix, args)); }
    warn(...args) { log.warn.apply(null, prepareLine('MCS W:' + this.prefix, args)); }
    error(...args) { log.error.apply(null, prepareLine('MCS E:' + this.prefix, args)); }
    static trace(...args) { log.trace.apply(null, prepareLine('MCS T:', args)); }
    static debug(...args) { log.debug.apply(null, prepareLine('MCS D:', args)); }
    static info(...args) { log.info.apply(null, prepareLine('MCS I:', args)); }
    static warn(...args) { log.warn.apply(null, prepareLine('MCS W:', args)); }
    static error(...args) { log.error.apply(null, prepareLine('MCS E:', args)); }
}
exports.Logger = Logger;
